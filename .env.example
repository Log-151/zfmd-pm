# 数据库连接 (必填)
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/zfmd_pm

# 会话密钥 (必填，请设置为足够随机的长字符串)
SESSION_SECRET=请修改为一个随机长字符串

# 运行端口 (Railway/Render 会自动注入，无需手动设置)
PORT=8080

# 运行环境 (production / development)
NODE_ENV=production
